
const int LM_current_sensor_pin = A0;
void setup() {
  // put your setup code here, to run once:
  pinMode(LM_current_sensor_pin, INPUT);
  Serial.begin(9600);
}

void loop() {
  // put your main code here, to run repeatedly:
  x = analogRead(LM_current_sensor_pin);
  Serial.print(x);
}
